name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  compose-validate:
    name: Validate Docker Compose
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Show docker compose version
        run: docker compose version

      - name: Detect compose file
        id: compose
        shell: bash
        run: |
          if [ -f docker-compose.yml ]; then
            echo "file=docker-compose.yml" >> "$GITHUB_OUTPUT"
          elif [ -f compose.yml ]; then
            echo "file=compose.yml" >> "$GITHUB_OUTPUT"
          elif [ -f deploy/docker-compose.yml ]; then
            echo "file=deploy/docker-compose.yml" >> "$GITHUB_OUTPUT"
          else
            echo "file=" >> "$GITHUB_OUTPUT"
          fi

      - name: Validate compose file (non-blocking first phase)
        if: ${{ steps.compose.outputs.file != '' }}
        continue-on-error: true
        run: docker compose -f "${{ steps.compose.outputs.file }}" config

      - name: Skip compose check
        if: ${{ steps.compose.outputs.file == '' }}
        run: echo "No compose file found, skip."

  frontend-build:
    name: Frontend Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Detect frontend
        id: fe
        shell: bash
        run: |
          if [ -f frontend/package.json ]; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Setup Node
        if: ${{ steps.fe.outputs.exists == 'true' }}
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install deps
        if: ${{ steps.fe.outputs.exists == 'true' }}
        working-directory: frontend
        shell: bash
        run: |
          if [ -f package-lock.json ]; then npm ci; else npm install; fi

      - name: Build
        if: ${{ steps.fe.outputs.exists == 'true' }}
        working-directory: frontend
        run: npm run build

  backend-test:
    name: Backend Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Detect backend
        id: be
        shell: bash
        run: |
          if [ -f backend/requirements.txt ] || [ -f backend/pyproject.toml ]; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Setup Python
        if: ${{ steps.be.outputs.exists == 'true' }}
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps (best effort)
        if: ${{ steps.be.outputs.exists == 'true' }}
        working-directory: backend
        continue-on-error: true
        shell: bash
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then
            pip install -r requirements.txt
          elif [ -f pyproject.toml ]; then
            pip install .
          fi
          pip install pytest

      - name: Run tests if tests dir exists (best effort)
        if: ${{ steps.be.outputs.exists == 'true' }}
        working-directory: backend
        continue-on-error: true
        shell: bash
        run: |
          if [ -d tests ]; then
            pytest -q
          else
            echo "No tests directory, skip."
          fi

      - name: Backend summary
        if: ${{ steps.be.outputs.exists == 'true' }}
        run: echo "Backend checks done (best effort)."
